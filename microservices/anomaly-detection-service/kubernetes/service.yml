apiVersion: v1
kind: Service
metadata:
  name: anomaly-detection-service
  labels:
    app: anomaly-detection-service
spec:
  type: ClusterIP
  selector:
    app: anomaly-detection-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9003
