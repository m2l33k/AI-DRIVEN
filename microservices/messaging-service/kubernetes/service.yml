apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  labels:
    app: messaging-service
spec:
  type: ClusterIP
  selector:
    app: messaging-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9007
