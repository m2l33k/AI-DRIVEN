eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
spring:
  cloud:
    discovery:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop  # fresh schema for each test run
