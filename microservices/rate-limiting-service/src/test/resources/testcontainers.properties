# Testcontainers configuration
# host.ip.override is intentionally NOT set here so Testcontainers auto-detects
# the Docker host on local dev (Docker Desktop) and in CI the
# TESTCONTAINERS_HOST_OVERRIDE env var (set by Jenkinsfile) takes precedence.
