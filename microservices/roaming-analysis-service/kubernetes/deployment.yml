apiVersion: apps/v1
kind: Deployment
metadata:
  name: roaming-analysis-service
  labels:
    app: roaming-analysis-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: roaming-analysis-service
  template:
    metadata:
      labels:
        app: roaming-analysis-service
    spec:
      containers:
        - name: roaming-analysis-service
          image: roaming-analysis-service
          imagePullPolicy: IfNotPresent
          lifecycle:
            preStop:
              exec:
                command: [ "sh", "-c", "sleep 5" ]
          ports:
            - containerPort: 9002
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: docker
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://eureka-server:8761/eureka/
