apiVersion: apps/v1
kind: Deployment
metadata:
  name: fault-injection-service
  labels:
    app: fault-injection-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fault-injection-service
  template:
    metadata:
      labels:
        app: fault-injection-service
    spec:
      containers:
        - name: fault-injection-service
          image: fault-injection-service
          imagePullPolicy: IfNotPresent
          lifecycle:
            preStop:
              exec:
                command: [ "sh", "-c", "sleep 5" ]
          ports:
            - containerPort: 9006
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: docker
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://eureka-server:8761/eureka/
