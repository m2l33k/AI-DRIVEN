apiVersion: v1
kind: Service
metadata:
  name: distributed-tracing-service
  labels:
    app: distributed-tracing-service
spec:
  type: ClusterIP
  selector:
    app: distributed-tracing-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9005
